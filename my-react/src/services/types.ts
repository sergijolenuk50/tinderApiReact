export interface IUserPostRequest {
    email: string;         // електронна пошта(обов'язкове поле)
    password: string;         // Унікальний ідентифікатор (обов'язкове поле)
    // description?: string; // Опис (необов'язкове поле)
}